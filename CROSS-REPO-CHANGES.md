# Cross-repo change report — planwith-fo-token

Generated: 2026-08-20 09:47
Port: 8084
Branch: develop
Domain short: token
MySQL schema: token_db / token_user

This file is generated by the add-repo scripts.
Review the schema plan below before push. Infra deploy then merges into the
already-running compose (same mysql as member/schedule) and re-ups together.

---

## 0. Schema plan (same MySQL container)

| Item | Value |
|------|--------|
| Domain | `token` |
| Database | `token_db / token_user` |
| Infra env example | `planwith-infra/env/token.env.example` |
| Service env example | `.env.example` (this repo, local `127.0.0.1:3307`) |
| Compose | existing `docker-compose.yml` (not a new stack) |

Tables are created by JPA `ddl-auto=update`. SQL only has CREATE DATABASE / USER.

---

## 1. New service repo (this folder)

| File | Status |
|------|--------|
| `Dockerfile` | created/updated |
| `.dockerignore` | created/updated |
| `.github/workflows/deploy.yml` | created/updated |
| `.env.example` | created (local JDBC) |
| `compose.snippet.yml` | created (infra snippet) |
| `gateway.route.snippet.yml` | created (Gateway, paste manually) |
| `CROSS-REPO-CHANGES.md` | this file |

GitHub repo: `planwith_planwith_fo_token` (team rule `planwith_<name>`)
Image: `planwith/planwith-fo-token:latest`

---

## 2. planwith-infra (other repo, already edited by the script)

- `env/token.env.example` - created
- `db/init/00-create-databases.sql` - added token_db / token_user
- `docker-compose.yml` - added service block planwith-fo-token
- `docs/PORTS.md` - regenerated
- `docs/LAST-ADD-REPO.md` - infra copy of this report
- AWS: `aws/service-registry.ps1` + `docker-compose.aws.yml` use the same Service name (ECR = Service). After GitHub repo exists: `scripts/register-aws-service.ps1 -ServiceName planwith-fo-token -InstallWorkflow`

Server secret env (`env/token.env`) is gitignored. After infra push, Deploy Infra copies the example only when that `.env` does not exist. Existing `.env` files are never overwritten.

Then Deploy Infra: apply SQL on the running MySQL (no volume wipe) and `compose up` the whole ready stack so the new service joins member/schedule.

---

## 3. Not done yet (other repo, manual)

### planwith_gateway (required to expose API on Gateway :8000)

The script does not modify the Gateway repo.

1. Open `gateway.route.snippet.yml` in this folder
2. Paste into `planwith_gateway/src/main/resources/application.yaml` in two places:
   - `spring.cloud.gateway.server.webflux.routes`
   - `springdoc.swagger-ui.urls`
3. Push Gateway → Actions deploy

Details: planwith-infra `docs/GATEWAY-DISCOVERY.md`

### planwith_discovery

No change. The service registers itself with Eureka.

---

## 4. Next actions (commit/push only if the user asked)

1. Commit/push this service repo (`develop`) → Windows image + AWS `Deploy AWS planwith-fo-token` (after `register-aws-service.ps1 -InstallWorkflow`)
2. Commit/push `planwith-infra` → compose merge + DB on running MySQL + stack re-up + AWS overlay
3. Paste Gateway snippet, then push Gateway
4. Check: Actions / Eureka `:8761` / Gateway `:8000` (do not call the service port from another PC)
5. Swagger Try it out Request URL must be `http://{SERVER_IP}:8000/...` — never a Docker hostname
